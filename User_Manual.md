# 📱 三星電腦螢幕 LINE 客服機器人 - 使用者操作手冊 (User Manual)

歡迎使用三星電腦螢幕 LINE 客服！服務範圍是三星電腦螢幕、Smart Monitor，以及手機、電腦或遊戲機連接螢幕的問題。

---

## 💬 互動功能與使用說明

手機版 LINE 聊天室下方有三個常駐入口，照編號使用即可：

- **① 直接問問題｜規格＆FAQ**：新問題一律先按這裡或直接打字，適合規格、功能與比較題；實驗期間每位使用者每天最多送出 20 題。
- **② 官方手冊重查｜回答不夠時・每日 5 次**：回答不足時再按，系統會顯示目前問題與型號；點「確認要查」後才讀 PDF、扣 1 次。若型號不對可點「換型號」；同系列有多個版本時會列出按鈕，不用自己找完整型號。
- **③ 網路解答重查｜需要實務做法時・每日 10 次**：按鍵本身就是授權；已有問題時會直接搜尋，不再多問一次確認。優先使用有引用可核對的非官方公開網頁，內容會標示「非官方，請斟酌參考」，且不會先讀 PDF 或把三星官網送進模型。若 Google 沒回傳可核對連結，仍會提供經安全過濾、清楚標示未證實的可能方向，不會只回「找不到答案」。
- 已確認的完整型號會保留到下次甚至數天後，除非你輸入新完整型號；一般使用者不需要 `/重啟`。
- 像 `G8` 這種同時代表多款 Odyssey 螢幕的系列名稱，系統會先列出完整型號讓你選。選定後只查該機種，不會把其他 G8 的功能或手冊混進來。
- 同時輸入兩個完整型號做比較時，系統只會使用這兩台各自的三星台灣規格；活動與贈品只採仍在有效期間內的資料，過期活動不會拿來回答。
- 問到其他品牌或非螢幕家電時，系統會直接說明服務範圍，不會沿用上一題螢幕型號；打錯或不存在的型號也不會被記成後續問題的產品。

點「直接問問題」會打開鍵盤；LINE 的鍵盤與 Rich Menu 不能同時顯示，收起鍵盤後選單仍在。按「查手冊」就是一次授權：已有上一題與型號便直接查；缺型號時只需選系列候選，選完就接著查，不會再問一次確認。真正送出 PDF 前才扣手冊次數；若精準 QA／官方規格／已核對片段已足夠，會免費直接回答並明示未讀整本 PDF。找不到手冊、查詢過大被系統阻擋、取消或逾時都不扣手冊次數。網搜只要供應商請求已送出就計 1 次；若沒有可核對引用，會以明確警語提供低風險可能方向，相同問題或同義改寫 10 分鐘內不會再次呼叫或扣次。按來源、取消、補型號與點選型號不計入每日 20 題。LINE 電腦版看不到常駐選單時，仍可輸入 `#查手冊` 或 `#搜尋網路`，行為與額度相同。

若你已確認查手冊、PDF 也已送出，但系統仍抓不到可核對頁面，系統會自動補搜一次非官方公開網頁，不必再按「搜網路」，也不扣你的每日 10 次網搜額度。補搜成功會清楚標示非官方來源；若仍沒有可靠引用，會給保守的可能處理方向與「到這款官網」，不會要你反覆重搜。

你可以直接在 LINE 中發送問題。系統會依照「先用免費且精準的本機資料，付費查詢前先問你」的順序回答：

1. **直接提問**：
   - 輸入型號或規格問題（例如：`c34g55t可以直接壁掛還是要轉接環`、`M8 螢幕有沒有喇叭`）。
   - 系統會先查精準 QA 與規格庫，不會一開始就讀 PDF。

2. **回答後按鈕（Quick Reply）**：
   - 在每則回覆的底部，會附帶動態功能按鈕：
     * 💬 **再詳細說明**：每題最多一次；有可信資料才補充。上一答沒有可核對資料時不呼叫 AI、不收費，會直接提供「查官方手冊／再查網路」。
     * 📖 **再查手冊**：沿用目前產品與問題，點下即授權並直接查；缺型號才先讓你選型號，不會再多問一次確認。
     * 🌐 **再搜網路**：回到同一套網路來源選擇，不會先查 PDF。
     * 🔗 **到這款官網**：只有系統已鎖定完整型號，而且目前回答、手冊或網搜仍不足時才出現；會開啟該款三星台灣產品頁或支援頁。系列別稱（例如 G8）尚未選型號時不顯示，正常且完整的 QA／規格答案也不顯示。

每次有效提問的 LINE 回覆底部只顯示一行簡版資訊，例如 `本次約 NT$0.0021｜直接問 19/20`、`手冊 4/5` 或 `網搜 9/10`。這是依 Gemini 官方 token 單價與系統實際用量換算的估值；完整 token、paidCalls 與 `[AUTO_*]` 內部標記只保留在管理者 LOG／TestUI Logs。

---

## 🛠️ 管理員指令

如果您是本專案的管理員或開發者（已綁定 LINE ID），您可以使用以下特殊指令：

* `/重啟`：
  - 只清空你的當前對話、待選來源與快取型號記憶。
  - 不重傳 PDF、不清空 QA／RULE，也不更新全體索引。一般 PDF 過期或上傳失敗由單檔修復、每日 04:00 重傳與背景自癒處理；只有對話真的卡在舊狀態時才需要人工 `/重啟`。

* `/紀錄 <內容>` 或 `/記錄 <內容>`：
  - 建立一筆草稿，不會立刻寫入資料表。
  - 一般客服問答會預覽為 `QA`；促銷、活動、價格、規格規則會預覽為 `CLASS_RULES`。
  - 若貼上 Samsung 活動網址，系統會讀取官方頁內容並整理活動 RULE；預覽不應只是一串網址。
  - 看完預覽後，單獨送出 `/紀錄` 或 `/記錄` 才會正式存檔並在背景更新知識庫。
  - 草稿期間直接回覆文字可修改內容；輸入 `/取消` 可放棄。

  範例：
  - `/紀錄 S27FG532EC 怎麼調整更新率？A：到遊戲選單調整更新頻率。`
  - `/紀錄 本期三星螢幕活動：S27FG532EC 促銷價 4990，活動期間 2026/07/01-2026/07/31，來源：https://promotion.twsamsungcampaign.com/...`
  - `/紀錄 https://promotion.twsamsungcampaign.com/2026-mnt-q2-sp/rule.aspx`

---

## 🧪 測試與驗證

專案提供了網頁版的「測試模擬器」，供您直接進行功能驗證：

1. **開啟測試模擬器**：
   - 在瀏覽器中訪問 Web App 部署網址並加上 `?test=1` 參數（例如：`https://script.google.com/macros/s/DEPLOY_ID/exec?test=1`）。
   - 您可以直接在網頁對話框中模擬使用者的提問，並在右側面板即時看到系統各個模組（DirectDeep, Smart Router, KB Select, AI Stats）的詳細服務端日誌。

2. **手冊索引與同步檢查**：
   - 訪問 `?pdfIndex=1`：可以即時取得系統內建的所有手冊型號索引（`PDF_MODEL_INDEX`）。
   - 訪問 `?manualCoverage=1`：取得 RULE 與 PDF 索引的缺口報表；TestUI 標題列也會顯示 `2026手冊：N/N` 或缺口數。索引暫不可用時只顯示「待檢查」，不會把所有型號誤報為缺手冊。兩者都必須使用維護憑證。
   - 訪問 `?sync=1`：可以立即強制同步 Drive 中的手冊，並以 1~2 秒超高速重建型號索引。
   - 訪問 `?driveFiles=1`：列出當前 Drive 資料夾中所有的 PDF 手冊名稱。

截至 2026-08-15 當次正式索引回讀，RULE 內 6 款 H／2026 世代型號（`S27H704EAC`、`S32HG802SC`、`S32HG806ES`、`S27HG806EF`、`S27HG802SC`、`S27HG612SC`）全部已有 PDF 基型號覆蓋，目前缺口為 0。
